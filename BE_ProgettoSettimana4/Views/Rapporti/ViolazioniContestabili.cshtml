@{
    var contestabili = ViewBag.Contestabili as IEnumerable<dynamic>;
}

<h1>@ViewBag.Titolo</h1>
<table class="table">
    <thead>
        <tr>
            <th>Importo</th>
            <th>Cognome</th>
            <th>Nome</th>
            <th>Data Violazione</th>
            <th>Decurtamento Punti</th>
            <th>Descrizione</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in contestabili)
        {
            <tr>
                <td>@item.Importo</td>
                <td>@item.Cognome</td>
                <td>@item.Nome</td>
                <td>@item.DataViolazione.ToString("dd/MM/yyyy")</td>
                <td>@item.DecurtamentoPunti</td>
                <td>@item.Descrizione</td>
            </tr>
        }
    </tbody>
</table>
